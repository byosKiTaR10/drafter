//Credenciales de la base de datos
const config = {
    db: {
        host: localhost,
        user: Oscar,
        password: pepesito123,
        database: h,
        connectTimeout: h
    },
}

module.exports = config;